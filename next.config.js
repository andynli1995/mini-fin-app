/** @type {import('next').NextConfig} */
const nextConfig = {
  // Note: The "_document not found" warning during build is a known Next.js 14 issue
  // when using App Router. It's harmless and doesn't affect the build output.
  // The build completes successfully despite this warning.
}

module.exports = nextConfig
